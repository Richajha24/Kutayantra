# Netlify Configuration for KŪṬAYANTRA Static Site Deployment

[build]
  publish = "."

# Security headers for Netlify
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
